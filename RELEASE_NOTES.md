官方插件 0.1.0 正式版，配套 Gamer 0.2.0。

- `tap` 可直接引用匹配结果，自动点击 `center`，无需再次识别。
- 可视化编辑器和资源保存接口检查已知引用类型，提前提示模板名、坐标等参数不匹配。
- 修复原生调用之间丢失画面尺寸导致点击和滑动偏移的问题；该修复随 Gamer 本体交付。
- 自动化要求宿主 input 1.1；旧本体拒绝安装，正式用户请先升级 Gamer 至 0.2.0（已安装 beta.7 的用户也具备该能力）。
- 自动化、键盘映射、视频工作台均为 0.1.0；功能代码与已发布 beta 保持一致，正式目录供 stable 宿主发现。
- `.gplugin`、合集 ZIP 和目录均提供 SHA256；不携带个人脚本和配置。

验收范围：beta 的自动化回归及实际发行包安装、更新、失败回滚已有通过记录；1920×1080 真机中心点击补充验收和对应保存拒绝复验尚未执行完成，不计为通过。维护者决定先发布正式版，后续问题以新补丁版本修复，不覆盖本版本资产。
