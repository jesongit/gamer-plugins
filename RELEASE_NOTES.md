官方插件及已验证的配套目录快照。安装单个 `.gplugin` 即可，无需安装 Git、Node 或 Rust。

`registry.json` 记录本次三款插件的确切版本、下载地址、权限、宿主 API 要求与哈希；`sha256sums.txt` 记录插件源码和 SDK 宿主提交。触发插件独立升版，其余插件保留各自版本。发布前请核对草稿资产并完成真实宿主安装验收。

host/ 或 builtin 执行体修改需要同时升级 Gamer；下载插件不会向旧宿主注入新的原生实现。
